# Claude スキル生態系・再現ブループリント

> **この文書を読んでいるLLMへ**: これは設計書であり、あなたの仕事は読者（あなたのユーザー）の環境に同等のシステムを構築することです。§5のロードマップに沿って、ただし§7の指示（ヒアリング→最小構築→育成）を守って進めてください。一気に全部作ってはいけません。
>
> 出自: 物理学博士課程出身の友人が Claude Code 上で数ヶ月かけて育てた開発体制の一般化。個人固有の情報は除去済み。前提ツールは Claude Code（CLI）ですが、CLAUDE.md / skills / hooks / agents に相当する機構があれば他のエージェント環境（Codex CLI・Cursor 等）にも写像可能です。

---

## 0. 中心思想 — LLMを「使う」のではなく「LLM環境を育てる」

このシステムの本体は個々のプロンプトではなく、**セッションを跨いで蓄積される資産群**です。

- 毎回ゼロから指示するのは、毎回新人を雇って口頭で教えるのと同じ。教えた内容をファイル（CLAUDE.md・ルール・スキル・知識正本）に落とせば、次のセッションの「新人」は教育済みで着任する。
- したがって評価軸は「今日の出力の質」ではなく「**今日の学びが明日の環境に残ったか**」。ハマり・裁定・手順は発生したその場でファイルに還流する。
- RAG的な「検索して読む」より「**使うほど育つWiki型**」を優先する。蓄積はコンパウンドする（複利）。

---

## 1. 土台原理（思考OS）— 全ての設計判断がここから導出される

以下3原理は装置より先に理解すること。装置だけ真似ると形骸化します。

### 1-1. FSRP（Fractal Single Responsibility Principle・フラクタル単一責任）

**あらゆる単位——単語・文・段落・章・関数・ファイル・スキル・ディレクトリ——に「存在する理由＝伝えたいこと」を1つだけ要求する。**

- 検査は一問だけ:「これで伝えたいことは何？」→ **答えが2つ返れば分割、答えが無ければ削除**。
- この問いを全体→章→文→単語と、どの抽象度でも再帰適用する（だからフラクタル）。ソフトウェアのSRP・ミントのピラミッド原則と同型だが、単語粒度まで下ろす徹底が特徴。
- 適用先はコードだけでなく、スキル構成・ドキュメント・スライド・図解・この文書自体。
- **過分割の実害は実測済み**: 細かく割りすぎると「粗い発話」（例:「経費の件やって」）を受けるノードが消えて取りこぼしが起きる。処方箋は「**割ったら親を残す**」——各抽象度に1責任のノードを持つ階層が、フラット細分割を支配する。

### 1-2. DP2L（Deterministic-Probabilistic 2-Layer・決定論と確率論の二層設計）

**機械（コード・lint・hook）の決定論的判定と、LLMの確率的判定を、対立ではなく補完として設計する。**

- 機械が担うもの: **保証・網羅・即時性・再現性**（例: 構文チェック、配布先の同期検査、閾値超えの検知）。
- LLM/人間が担うもの: **意味の判定**（例: この分割は妥当か、この文は責任1つか）。
- 実務ルール:「**機械は兆候の検知（黄色信号）まで・有罪判定は意味の層**」。lintが緑でも意味の歪みはありうるので「lint緑＝クリーン」と誤読しない。逆に、LLMに数え上げや網羅をやらせない（機械の仕事）。
- LLMの再現性の無さは欠陥ではなく表現の幅の源泉。ただし「統計的に収まる範囲」を検証装置（eval・受け入れテスト・回帰）で測ることを前提に使う。

### 1-3. 理想理論はズレ検出器

**シンプルで美しい理想構造（原則・規約・理想の型）を作る目的は、それへの到達ではなく、現実とのズレを可視化する座標系を得ること。**

- 物理の摂動論と同じ構図: 理想気体・調和振動子は結論ではなく基底で、ズレの項こそが対象の固有情報。
- 実務形: 理想の型から導出できない資産＝設計の欠落として検出される。可視化（図・診断レポート）は納品物ではなく**検証装置**。
- 帰結: 原則への完全準拠を目指して疲弊しない。原則は計測器であり、ズレを見つけたら直すか「なぜズレてよいか」を記録する。

---

## 2. 常設資産 — セッションを支える7つの装置

| # | 装置 | 一言で | 実体（Claude Codeの場合） |
|---|---|---|---|
| 1 | 配線 | 何をいつ読むかの案内板 | CLAUDE.md（グローバル＋プロジェクト） |
| 2 | 記録 | プロジェクトの魂と学びの置き場 | docs/ 5点セット |
| 3 | 横断ルール | プロジェクト横断規律の正本 | ~/.claude/rules/*.md |
| 4 | 技能 | 繰り返し作業の型のパッケージ | ~/.claude/skills/（SKILL.md） |
| 5 | 反射 | 失敗署名を検知する機械の番人 | ~/.claude/hooks/*.py |
| 6 | 専門家 | 常設の相談役エージェント | ~/.claude/agents/*.md |
| 7 | 知識正本 | 調査結果の引用付き蒸留 | canons/（+ NotebookLM等の調査基盤） |

### 2-1. 配線: CLAUDE.md体系

**原則:「案内板は魂を持たない」＝CLAUDE.mdは正本ではなくポインタ集。**

- グローバルCLAUDE.md（全プロジェクト共通）に置くもの: ユーザーのプロフィールと接し方・役割分担・**「運用ルールの正本一覧」表**（迷ったらどのファイルを引くかの1行×N）。ルール本文はここに書かず rules/ やスキルに置き、表から指す。
- プロジェクトCLAUDE.mdに置くもの: **セッション開始時の読み込み表**（どのdocsをどの順で・どこまで読むか）＋プロジェクト固有差分のみ。
- 太らせない。本文をコピーで重複させると乖離事故が起きる（→§3-1 SSOT）。50KB超のファイルは目次だけ読む等の安全弁も明記。

### 2-2. 記録: docs 5点セット（プチ仕様駆動）

**順序が本体: PLAN → SPEC → TODO → KNOWLEDGE →（それから）実装。「魂を先に作る・いきなり作らない」。**

| ファイル | 責任 |
|---|---|
| PHILOSOPHY.md | なぜやるか・判断軸（③の魂。案内板から指されるだけの正本） |
| PLAN.md | 背景・目的・制約・**やらない選択肢とその理由** |
| SPEC.md | 仕様・doneの定義・技術選定の根拠・棄却済み選択肢 |
| TODO.md | **残タスクのみ**（完了エントリは削除。履歴はgit logと報告書へ） |
| KNOWLEDGE.md | エラーと対処・棄却した設計案と理由。**7装置の中で最も価値が高い** |

- KNOWLEDGEが最重要な理由: LLMは同じ穴に何度でも落ちる。ハマりを1回で資産化すれば2回目以降は読み込み表経由で回避される。追記は「発生したその場」で行う（後回しにすると消える）。
- 大きい作業は `docs/instructions/active/<slug>.md`（実装指示書）→ 実装 → `reports/YYYY-MM/`（完了報告書）のパイプラインにする。設計と実行をセッション（コンテキスト）で分離でき、指示書と報告書自体が前例資産になる。

### 2-3. 横断ルール: rules/

プロジェクトを跨ぐ規律（テストの流儀・デプロイ標準・調査の手順・モデル選択基準など）は1ドメイン1ファイルで rules/ に置き、グローバルCLAUDE.mdの正本一覧表から指す。

- **罠（実測）**: 「関連ファイルを触るときだけ読み込む」式の条件付きロードは、パス署名を持たないドメイン（例: 調査ルール）に付けると**一度も発火せず規律が静かに消える**。無条件ロードが必要なルールに条件を付けない。この不変条件をファイル冒頭に刻んでおく。

### 2-4. 技能: skills

繰り返し作業の型を SKILL.md（frontmatterの name / description ＋ 手順本文）としてパッケージする。LLMがユーザーの発話から description を見て自動発火する。

**設計原則 = 関数設計との写像**（これがこのシステムの核心の一つ）:

| 関数設計 | スキル群での形 |
|---|---|
| 単一責任 | 1スキル1責務・1ファイル1軸（FSRP） |
| サブルーチン抽出 | 複数の消費者が現れた工程を独立スキルへ |
| Rule of Three | **2人目の消費者が現れるまで切らない**（1人のうちは「名前だけ予約」） |
| Functional Core, Imperative Shell | **層A＝純粋（汎用・対象非依存・読むだけ）／層B＝IO（ユーザー固有データを読む・書く・判定を取る）** |
| 依存方向の規律 | 層A→層Bへの依存禁止（汎用コアは個人データを知らない） |
| DRY/SSOT | 本体の二重化禁止・要約＋ポインタは推奨 |

**関数の類推が壊れる場所**（ここを無視すると細分化事故）:
1. **ディスパッチが確率的**: 関数はcall siteで決定的に呼ばれるが、スキルはLLMがdescriptionから発動判定する。分割するほど誤ディスパッチ率が上がる＝分割にはコストがかかる。粒度の下限は「**ユーザーの発話で区別できるか**」。
2. **呼び出しが重い**: スキルのRead連鎖は数千トークン。頻用規律は呼び出し側に要約を転記し正本へポインタ。
3. **コンパイラがいない**: 構造変更の波及は型チェックで検出されない → grep総ざらい＋機械lint（§2-5）が型チェックの代用。

**description の書き方（Trigger Triad）**: ①何ができるか（動詞＋目的語）②いつ使うか（「〜のとき使う」）③ユーザーが実際に打つ語彙の列挙。さらに**過剰発動には `Do NOT use for:` で否定境界**を書く。新設・改修時は受け入れテスト（§3-3）を通す。

**スキルはスキルを呼ぶ（合成）**: 成熟すると、スキルは単発の型ではなく関数合成のグラフになる。原本環境の実例——最長チェーンは「新repo鋳型 → 博士ファクトリー → 正本ファクトリー → 意図確定ループ → 本質抽出エンジン」の5段（setup-project → make-hakase → make-canon → goal-seek → abstract-core）。見どころは4段目:
- **曖昧さゲート**: 正本ファクトリー（make-canon）は、依頼が「依頼者と照合済み」フラグ付きでない限り、着手前に意図確定ループ（goal-seek）を必ずかます。理由は実測されたバグ——曖昧な依頼を製造側が自前解釈で補うと、**依頼者の広い意図を勝手に狭く解釈してスコープを固定する silent な欠落**が起きる（例:「CAに必要な情報」→面談実務に狭化し事業運営層が丸ごと落ちた）。
- 判定軸は「観点が明示されているか」ではなく「**依頼者と照合済みか**」。この2つを同一視した旧条文は、上流スキル経由の依頼がゲートに一度も掛からない穴になっていた（発見→条文修正の経緯ごとKNOWLEDGEに記録）。
- 教訓として一般化すると: **下流の製造スキルには「入力の照合状態」を型として持たせ、未照合なら上流の意図確定スキルへ差し戻す**。関数でいう入力バリデーションのスキル版。

### 2-5. 反射: hooks（決定論層の番人）

hookはセッションイベント（開始・ツール実行前後・停止）に発火するスクリプト。LLMの「気をつけます」は構造的に信用できないため、**繰り返す失敗はhookで機械化する**。

- 型:「**失敗の署名**（失敗が起きる直前の観測可能な行動パターン）を検知して合図する」。例: 前例があるのにフル探索を始めた瞬間・コンテキストが閾値を跨いだ瞬間・編集後の構文エラー。
- 作法: **セッション初回のみ合図して以後沈黙する自己回復型**にする（毎回ブロックすると邪魔になり無効化されて終わる）。強制ブロックは本当に危険な操作だけ。
- hook・agent・skillの台帳（META.md等）を作り、**整合性lint**（frontmatter有無・正本と配布先の同期＝parity検査）を機械で回す。これがDP2Lの決定論層。

### 2-6. 専門家: agents（博士パターン）

特定分野の相談が繰り返し発生するなら、その分野の「博士」agentを常設する。

- 博士＝**引用付き知識正本（§2-7）を根拠に答えるドメイン相談役**。記憶で答えさせず、正本とその出典に接地させる。「公式の明言」と「実測・推測」を峻別させる。
- **critic**（独立コンテキストで批判レビューするagent）を1本持つと効く: 成果物を作った文脈・意図を渡さず、成果物とチェックリストだけ渡して叩かせる（自己レビューは投資バイアスで甘くなる）。
- **モデルティアリング**: 推論勝負（設計・批判・統合）は上位モデル、幅の作業（検索・収集・要約）はagentのfrontmatterで下位モデルにピン。検索agentを量産する調査系は特に効く。

### 2-7. 知識正本: canon ＋ 調査基盤

「調査して」の結果を使い捨てにせず、**引用付きの正本md（canon）に蒸留して資産化**する。

- 工程: テーマ専用のノートブック（NotebookLM等）を常設 → 観点別に複数ラウンド調査 → ソースを信頼度で**層化**（一次資料／独立体験記／事業者発信＝要割引／ゴミ。**削除でも要約でもなく層化**——ゴミ1割のために針を消さない）→ 意思決定に直結する問いを投げて引用付きで蒸留 → canonファイルに確定パス＋ノートブックURLを記録。
- canonは博士agentの根拠・スキルの参考文献・執筆の裏取りに使い回される（消費者が複数いる知識の置き場）。
- 注意: **鮮度が速い事実（料金・モデルID等）はcanonに写さず、更新される外部正本を参照させる**。写した瞬間から腐り、誰も直さない。

---

## 3. 運用規律 — 人とAIの回し方

### 3-1. SSOT（正本は1箇所）

- 全ての文書・スキルは「正本1箇所＋他は要約とポインタ」。本体コピーは乖離事故の温床。
- スキルの正本ディレクトリ（skills-src/ 相当）を1つ立て、配布先（~/.claude/skills/ 等）へはスクリプトで反映。**配布先を直接編集しない**（次の反映で消える）。正本と配布先の一致はlintで機械検査。

### 3-2. 2人目の消費者ルール（過分割の予防）

- 共通化・スキル切り出しは**2人目の消費者が現れてから**。1人のうちは切らず「名前だけ予約」しておく。
- 切るときのチェックリスト: ①2人目はいるか ②切る軸は1つか（1ファイルに2軸を同居させない）③純粋部とIO部が混ざっていないか ④発話で区別できる粒度か ⑤正本は1箇所か ⑥**粗い発話の受け皿（親ノード）は残っているか**。

### 3-3. 受け入れテスト（スキルの発火品質）

新設・description改修時: **発動すべき5〜7＋発動すべきでない3〜5＋境界2〜3の自然文プロンプト**を用意し、発動判定を採点。発動漏れ→同義語追加、過剰発動→Do NOT追加。**責務レベルの曖昧発話（「〜の件よろしく」型）を必ず含める**（過分割の実害はここの取りこぼしに現れる）。

### 3-4. N回目チェック（前例retrieveの強制）

着手前に一段抽象化して「**これは何という種類の作業のN回目か**」を自問。N≥2なら、フル探索の前に前例（過去の成果物・指示書・報告書・スキル）をretrieveし、**差分だけを計画**する。LLMは「想起より再発見」に流れやすく、既にやった作業を新規問題としてフル探索して時間を溶かす——そのメタ対策。工程（テンプレに「前例」欄）とhookの両方で強制する（片方では漏れる）。

### 3-5. 自走の既定＋ハードストップ

- 既定は**実行・デプロイまで自走**（証拠＝コマンド・出力・スクショを後出しレビュー）。「人が目視したいだけ」は停止理由にしない。テスト緑で止めない。
- ただし次の3カテゴリだけは直前でdry-run/プレビューを出して承認を待つ: **①本番データへの書き込み ②外部の人への送信 ③金銭が動く操作**。境界を先に文書化しておくと、LLMは残り全部を安心して自走できる。
- 実装中に仕様・UXのトレードオフ判断（人間の領分）が出たら止めて確認。技術選定はLLM、ビジネス判断は人間、と階層を先に切っておく。

### 3-6. セッション分割・トークン規律

- **実測: 週次利用枠の消費の9割超はコンテキスト再送**（会話が長いほど毎リクエスト全量再送＝総消費はセッション長の二次）。モデルやreasoningを絞るよりセッション分割が桁で効く（100リクエスト毎の分割で総入力-47%）。
- 実務の型: **1タスク1セッション**。切る判断は「今の長さ」でなく「残り往復数×削減量 > 積み直しコスト」。切る前に決定事項・未決・次アクションの3点をdocsに落とす。
- サブエージェントに調査させ**蒸留結果だけ**メインコンテキストに受け取る（生データを積まない）。

---

## 4. ケーススタディ: 分身プロジェクト — 層A/層Bアーキテクチャの実物

§2-4で書いた「層A（汎用）／層B（個人特化）の分離」が最も濃く出ている応用例。この生態系の技術的な到達点なので、原理の実演として1章割く。

### 4-1. 何を作るプロジェクトか

**ユーザーという関数 f(状況)→出力 をファイル群で再現する**——判断基準・境界・アンチパターンを「人格データ」として外部化し、本人の代わりに判断を先出しするエージェントを作る。

- 段階設計が肝: いきなり無人代行を狙わない。まず「**判断の検分器**」＝分身が「本人ならこう決める＋理由」を先出しし、本人の実判断と照合して一致の実績を貯める。信頼が貯まった領域から委任を広げる。ハードストップ3カテゴリ（§3-5）は常にエスカレーション。
- 用途の限定も肝: 分身は**一方向の関数適用**であり、壁打ち相手ではない。自分の複製と往復議論しても情報利得はゼロ——新視点が欲しければ専門家（博士）か批判役（critic）を呼ぶ。

### 4-2. 4つの部品と依存方向

| 部品 | 層 | 責任 |
|---|---|---|
| **abstract-core** | 層A・純粋コア | 判断・発言・選好を価値観/原則へ抽象化する汎用エンジン。**ユーザーを知らない** |
| **persona-interview** | 層B・IO殻 | 過去の判断を掘り、本人の判定を経て人格データへ書き込む**唯一の書き手** |
| **goal-seek** | 層A・IO殻 | ゴール/依頼/修正指示から「効く変数」を洗い出し、未確定を1問ずつ質問で埋める汎用ループ |
| **分身エージェント** | 層B・inference | 人格データを読み、goal-seekを「参照先＝人格データ（読取のみ）」で起動する薄い配線層 |

- **abstract-coreの中身**: ゼロから発明せず、認知科学の既存技法を移植している——CDM（Critical Decision Method・専門家の暗黙判断を掘る面接法）・ラダリング（why連鎖で価値観まで遡る）・構造化質問法・**後付け虚話（confabulation）検出**（人は自分の判断理由を後付けで捏造するので、理由の言明をそのまま信じない）。
- **レンズ＝アダプタ構造**: コアは1つ、対象別のレンズ（人の判断レンズ／タスク構造レンズ）を差し替える。ストラテジーパターンそのもので、**訓練・改善の対象はコアだけ＝改善が全レンズに複利で効く**。1問い1レンズ、1ファイルに2軸（レンズ＋聞き方）を同居させない。
- **書込ゲート**: 抽出結果は本人の判定（member-checking: 合ってる／違う／「おっ」＝本人も気づいてなかった）を通った分だけ人格データに入る。抽出①と確認②はレイヤー分離し、書込経路は persona-interview の1本だけに絞る。

### 4-3. ML実務との同型（一番速い説明）

- persona-interview＝**train**（データ収集＋本人ラベリング）／分身エージェント＝**inference**／判断再現テスト＝**eval**。
- **eval汚染の禁止**: ホールドアウトした過去判断セットは分身にも人格データにも絶対に流さない（テストデータをtrainに混ぜる事故と同じ）。分離をスキルのDo NOTに明文化してある。
- 層A/層B分離＝モデルとデータの分離。汎用コアは個人を知らないので、他人の判断分析（歴史上の人物でも）にそのまま当たる。

### 4-4. なぜこの形に育ったか（設計原則の実演）

- abstract-coreは最初、persona-interviewの内部工程だった。**goal-seekという2人目の消費者が現れた時点で切り出された**（§3-2の実演——最初から「汎用エンジンを作ろう」と切ったのではない）。
- その後「レンズと聞き方が1ファイルに同居している」歪みが見つかり純化手術。装置が先ではなく、**原理（FSRP・層分離）が先にあって装置が従う**、の典型例。

---

## 5. 構築ロードマップ — この順で作る（飛ばさない）

各Phaseは前のPhaseの「痛み」が動機になる設計。痛みが出る前に作った装置は形骸化します。

### Phase 0: 1プロジェクトに配線と記録を敷く（初日）
1. グローバルCLAUDE.md: ユーザーのプロフィール・接し方・自走の既定とハードストップ3カテゴリ。
2. 対象プロジェクトに docs 5点セット（PHILOSOPHY/PLAN/SPEC/TODO/KNOWLEDGE）の骨組みと、プロジェクトCLAUDE.mdの読み込み表。
3. PLAN→SPECを人間との壁打ちで埋めてから実装に入る。

### Phase 1: KNOWLEDGE還流の習慣化＋最初のスキル1本（1週目）
1. ハマるたびにKNOWLEDGE.mdへ即追記（目次1行＋詳細セクション）。
2. 「3回以上繰り返した作業」を1つ選び、最初のSKILL.mdにする。description受け入れテストも回す。
3. **候補の選び方: 一番しんどい部分だけ半自動化**。全自動化を狙わない。

### Phase 2: SSOT化と機械の番人（2〜4週目）
1. スキルが3本を超えたら正本ディレクトリ（skills-src）＋配布スクリプト＋台帳（README表）を立てる。
2. 最初のhook: 自分が2回以上繰り返した失敗の署名を1つ選んで検知させる（初回のみ合図・自己回復型）。
3. 整合性lint（frontmatter・配布parity）を作り、週次かhookで回す。

### Phase 3: 知識のファクトリー化（2ヶ月目〜）
1. 調査基盤（NotebookLM等）にテーマ別ノートブックを常設し、調査→層化→引用付きcanonの工程を1回通す。
2. 相談が繰り返される分野に博士agent（canon接地）を1本。criticも1本。
3. canon製造・博士製造の工程自体をスキル化（メタスキル）——「装置を作る装置」で複利が立ち上がる。

### Phase 4: 自己改善ループ（3ヶ月目〜）
1. 構造の違和感（「これ2箇所にない？」「レイヤー揃ってる？」）が出たら診断→切る基準の1行化→分割/統合/改名→grep総ざらい検証、の構造手術を工程化。
2. 構造・体制の可視化規格（ツリー図の雛形＋ノード規約）を1本固定し、毎回同じ見た目で出す。
3. 定期メタレビュー: hook/agent/skill台帳と実物の突合・使われていない資産の棚卸し。

---

## 6. ハマり集 — 授業料の先渡し（一般化済み）

1. **サブエージェントの第1報には実在しないファイル名が混入しうる**。生命線に関わるリスト（削除対象・秘密ファイル等）はGlob/gitで一次確認してから信じる。
2. **「無い」の誤診は繰り返す**。要約経路は多段の表を静かに切り落とす。存在確認は検索コマンドの実行結果で行う（LLMの「見当たりません」を根拠にしない）。
3. **投入・登録系APIにはsilent失敗がある**。「投入済み」と記録する前に実在確認を1回挟む。
4. **機械lintの緑をクリーンと誤読しない**。lintは各ノードの規律まで、枝をまたぐ重複・分類軸のMECE性は意味の層（critic/人間）の仕事。
5. **外部ツールのdescription・設定は更新で消える**。上書きされる場所に判断を書かず、上流（自分のCLAUDE.md）に優先順位を1行書く。
6. **AIジャッジだけで文章系のA/B優劣を決めない**。最終判定は人間の読感を上位に置く。
7. **hookの効果測定は「出なくなったか」でなく「失敗が減ったか」**。合図を増やしすぎると全部が無視される。合図は1セッション1回まで。

---

## 7. 構築を任されたLLMへの指示

1. **まずヒアリング**（構築より先）: ①週に3回以上繰り返している作業は何か（→最初のスキル候補）②絶対に自動でやってほしくない操作は何か（→ハードストップ3カテゴリの具体化）③判断に迷ったとき何を優先するか（→PHILOSOPHYの種）④普段どんな言葉で依頼するか（→descriptionの語彙）。
2. **Phase 0だけ作って止まる**。以降は実際の作業で痛みが出るたびに、該当Phaseの装置を1つずつ足す。7装置を初日に全部作るのは§3-2（2人目の消費者ルール）違反であり、確実に形骸化する。
3. **ユーザーの語彙で書く**。descriptionもルールも、ユーザーが実際に打つ言葉・実際に犯す失敗から逆算する。この文書の固有名詞（FSRP等）をそのまま輸入する必要はない——原理の構図だけ写し、名前はユーザーと決める。
4. **全ての新資産に「なぜ存在するか」を1行書く**（FSRPの実践）。書けなければ作らない。
5. 学びが出るたびKNOWLEDGEへ。**この文書自体も読者の環境で育てる対象**であり、完成品ではない。

---

*2026-07-31 作成。原本の環境規模（参考値）: グローバルスキル19本・hook20本・agent10種・canon10冊・横断ルール6本。ここまで約4ヶ月。*
